name: J文件夹 物理极限对拷 (终极完全体)

on:
  workflow_dispatch:

jobs:
  transfer:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: 1. 物理机底层网络魔改 (注入 BBR)
        run: |
          echo "=== 💥 正在解除 Ubuntu 虚拟机网络封印 ==="
          sudo sysctl -w net.core.default_qdisc=fq
          sudo sysctl -w net.ipv4.tcp_congestion_control=bbr
          sudo sysctl -w net.core.rmem_max=67108864
          sudo sysctl -w net.core.wmem_max=67108864
          sudo sysctl -w net.ipv4.tcp_rmem="4096 87380 67108864"
          sudo sysctl -w net.ipv4.tcp_wmem="4096 65536 67108864"
          sudo sysctl -w net.ipv4.tcp_mtu_probing=1
          ulimit -n 65535

      - name: 2. 部署 Rclone 核心环境
        run: curl https://rclone.org/install.sh | sudo bash

      - name: 3. 动态构建挂载点 (填入真·底层接口)
        env:
          PIKPAK_USER: ${{ secrets.PIKPAK_USER }}
          PIKPAK_PASS: ${{ secrets.PIKPAK_PASS }}
          PAN123_USER: ${{ secrets.PAN123_USER }}
          PAN123_PASS: ${{ secrets.PAN123_PASS }}
        run: |
          echo "=== 🛠️ 组装 PikPak 节点 ==="
          rclone config create pikpak pikpak user "$PIKPAK_USER" pass "$PIKPAK_PASS"
          
          echo "=== 🛠️ 组装 123云盘 WebDAV 节点 ==="
          # 这里已经替换为你提供的最准确的底层数据通道接口
          rclone config create 123pan webdav url "https://webdav.123pan.cn/webdav" vendor other user "$PAN123_USER" pass "$PAN123_PASS"

      - name: 4. 预检测防阻断 (强制铺设目录)
        run: |
          echo "=== 📁 正在 123云盘 开辟专属阵地 ==="
          rclone mkdir "123pan:/视频/j" || true

      - name: 5. 满血引擎启动 (极致并发 + 瞬时断点续传)
        run: |
          # 核心压榨指令，无视已传文件，直取目标
          rclone copy "pikpak:/j" "123pan:/视频/j" -v \
            --transfers 24 \
            --checkers 48 \
            --buffer-size 128M \
            --multi-thread-streams 4 \
            --use-mmap \
            --size-only \
            --ignore-existing \
            --tpslimit 12 \
            --tpslimit-burst 12 \
            --low-level-retries 20 \
            --retries 15 \
            --timeout 10m \
            --contimeout 10m \
            --stats 15s \
            --stats-one-line
